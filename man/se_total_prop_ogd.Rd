% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/se_ogd.R
\name{se_total_prop_ogd}
\alias{se_total_prop_ogd}
\title{Estimate Totals and Proportions for All Combinations of Grouping Variables (OGD Format)}
\usage{
se_total_prop_ogd(data, ..., strata, weight, alpha = 0.05)
}
\arguments{
\item{data}{A data frame or tibble containing the survey data.}

\item{...}{Grouping variables (unquoted or programmatic) to compute combinations of totals and proportions.}

\item{strata}{Stratification variable (unquoted or programmatic). Defaults to \code{"zone"} if omitted.}

\item{weight}{Sampling weight variable (unquoted or programmatic).}

\item{alpha}{Significance level for confidence intervals. Default is 0.05 (for 95\% CI).}
}
\value{
A tibble with totals and proportions for all combinations of the specified grouping variables.
The output includes confidence intervals and handles missing values by representing them as "Total".
}
\description{
\code{se_total_prop_ogd} estimates totals and proportions for each combination
of grouping variables using \code{se_total_prop}, returning results in a format compatible with Open Government Data (OGD) standards.
along with stratification and weighting.
}
\examples{
# With unquoted variables
se_total_prop_ogd(nhanes, gender, birth_country, strata = strata, weight = weights)

# Programmatic usage
vars <- c("gender", "birth_country")
wt <- "weights"
se_total_prop_ogd(nhanes, !!!rlang::syms(vars), strata = strata, weight = !!rlang::sym(wt))

}
\seealso{
\code{\link{se_total_prop}}, \code{\link{se_ogd_wrapper}}, \code{\link{se_total_ogd}}, \code{\link{se_prop_ogd}}
}
