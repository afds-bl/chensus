% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/se_mean_comb.R
\name{se_mean_comb}
\alias{se_mean_comb}
\title{Estimate Averages of Numeric Variables for All Combinations of Grouping Variables in Structural Survey}
\usage{
se_mean_comb(data, variable, ..., strata, weight, alpha = 0.05)
}
\arguments{
\item{data}{A data frame or tibble.}

\item{variable}{Unquoted or quoted name of the numeric variable whose mean is to be estimated.
Programmatic usage (e.g., using \code{!!sym()}) is supported.}

\item{...}{Optional grouping variables. Can be passed unquoted (e.g., \code{gender}, \code{birth_country}) or programmatically using \code{!!!syms(c("gender", "birth_country"))}..}

\item{strata}{Unquoted or quoted name of the strata column. Defaults to \code{zone} if omitted.}

\item{weight}{Unquoted or quoted name of the sampling weights column. For programmatic use.
with a string variable (e.g., \code{wt <- "weights"}), use \code{!!sym(wt)} in the function call.}

\item{alpha}{Numeric significance level for confidence intervals. Default is 0.05 (95\% CI).}
}
\value{
A tibble combining survey estimates for all combinations of \code{group_vars},
  with grouping variables converted to factors where "Total" means no grouping.
}
\description{
\code{se_mean_comb()} computes structural survey means for every combination of the supplied grouping variables.
This wrapper function uses \code{\link[=se_mean]{se_mean()}} and the helper function \code{\link[=se_combn]{se_combn()}} and returns a combined tibble with results
for each grouping subset, including means across all groups. This formatting can be useful for Open Government Data platforms.
}
\examples{
se_mean_comb(
  nhanes,
  variable = household_size,
  strata = strata,
  weight = weights,
  gender, interview_lang
)

}
\seealso{
\code{\link[=se_mean]{se_mean()}}, \code{\link[=se_combn]{se_combn()}}.
}
